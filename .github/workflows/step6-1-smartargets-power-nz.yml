name: "Step 6.1: SMARTargets — Power Sector NZ"

# Ambitious Transition: Power sector net-zero emission constraint.
# Hard cap on emissions as % of 2023 eGRID absolute emissions per ISO.
# Trajectory: 2030=43%, 2035=18%, 2040=12%, 2045=6%, 2050=0%.
#
# Prerequisite: Step 3 cost optimization parquets + data/egrid_2023_baseline_emissions.json
# Output: data/step10-smartargets/smartargets_{scenario}.parquet + _summary.json

on:
  workflow_dispatch:
    inputs:
      scenario:
        description: 'Scenario to simulate'
        required: true
        default: 'ALL'
        type: choice
        options:
          - ALL
          - AT1
          - AT2
      iso:
        description: 'ISO region (or ALL)'
        required: true
        default: 'ALL'
        type: choice
        options:
          - ALL
          - CAISO
          - ERCOT
          - PJM
          - NYISO
          - NEISO
          - MISO
          - SPP
      branch:
        description: 'Branch to checkout'
        required: false
        default: 'master'
        type: string

env:
  BRANCH_PREFIX: step10b-smartargets-power-nz

jobs:
  smartargets-power-nz:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch || 'master' }}
          fetch-depth: 0

      - name: Create output branch
        run: |
          TIMESTAMP=$(date -u +%Y%m%dT%H%M%S)
          SCENARIO_LABEL="${{ inputs.scenario }}"
          ISO_LABEL="${{ inputs.iso }}"
          TARGET_BRANCH="auto/${TIMESTAMP}-${BRANCH_PREFIX}-${SCENARIO_LABEL}-${ISO_LABEL}"
          echo "TARGET_BRANCH=$TARGET_BRANCH" >> "$GITHUB_ENV"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "$TARGET_BRANCH"

      - name: Verify upstream data exists
        run: |
          if [ ! -d "data/step3-cost-opt-parquets" ] || ! ls data/step3-cost-opt-parquets/*.parquet >/dev/null 2>&1; then
            echo "ERROR: No step-3 parquets found. Run Step 3 first."
            exit 1
          fi
          if [ ! -f "data/egrid_2023_baseline_emissions.json" ]; then
            echo "WARNING: eGRID baseline not found — will use hardcoded fallback values"
          fi

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install numpy numba pyarrow pandas scipy

      - name: Ensure output directory
        run: mkdir -p data/step10-smartargets

      - name: Build CLI arguments
        run: |
          ARGS=""
          if [ "${{ inputs.scenario }}" = "ALL" ]; then
            ARGS="--scenarios AT1 AT2"
          else
            ARGS="--scenarios ${{ inputs.scenario }}"
          fi
          if [ "${{ inputs.iso }}" != "ALL" ]; then
            ARGS="$ARGS --isos ${{ inputs.iso }}"
          fi
          echo "SMARTARGETS_ARGS=$ARGS" >> "$GITHUB_ENV"
          echo "Running: python scripts/step6_1_smartargets.py $ARGS"

      - name: Run SMARTargets simulation
        run: python scripts/step6_1_smartargets.py ${{ env.SMARTARGETS_ARGS }}

      - name: Commit and push results
        run: |
          git config http.postBuffer 524288000

          push_with_retry() {
            local label="$1"
            local MAX_RETRIES=4
            for i in $(seq 1 $MAX_RETRIES); do
              echo "  Push attempt $i/$MAX_RETRIES for ${label}..."
              git push -u origin "$TARGET_BRANCH" 2>&1 && return 0
              if [ "$i" -eq "$MAX_RETRIES" ]; then
                echo "  ERROR: Push failed for ${label} after $MAX_RETRIES attempts"
                return 1
              fi
              BACKOFF=$((2 ** i))
              echo "  Retrying in ${BACKOFF}s..."
              sleep "$BACKOFF"
            done
          }

          git add -f data/step10-smartargets/
          if ! git diff --cached --quiet; then
            git commit -m "Step 10B SMARTargets Power NZ: ${{ inputs.scenario }} × ${{ inputs.iso }} — $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            push_with_retry "smartargets-results"
          else
            echo "No changes to commit"
          fi
