# hourly-cfe-optimizer Python dependencies
#
# Install all:  pip install -r requirements.txt
#
# Numba JIT is critical for performance — without it, Step 3 cost
# optimization falls back to pure numpy and runs 10-50× slower.
# Verify after install:  python3 -c "from numba import njit; print('OK')"
#
numpy>=1.24.0
numba>=0.59.0
pyarrow>=15.0.0
pandas>=2.0.0
