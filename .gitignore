# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Environment
.env
.env.local

# Optimizer generated data (large binary files — old JSON checkpoints only)
data/checkpoints/CAISO_cache_*.json
# Checkpoint JSONs grow huge during Step 1 runs (full threshold data embedded)
# Strip to just completed list before committing, or gitignore them
data/checkpoints_v4/*_checkpoint.json
# Note: data/checkpoints_v4/*.parquet must NOT be ignored — bank interim results
# CRITICAL: Do NOT gitignore physics_cache_v4.parquet — it must be committed
# to prevent data loss across sessions. Previous loss of 21M solutions was
# caused by gitignoring this file.
dashboard/physics_results_v4.json

# Large JSON results (>100MB, exceed GitHub limit) — use parquet versions instead
# JSON versions are regenerated locally from parquets via convert_parquet_to_json.py
dashboard/overprocure_results.json
dashboard/demand_growth_results.json
